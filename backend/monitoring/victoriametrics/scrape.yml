global:
  scrape_interval: 15s
  scrape_timeout: 10s

scrape_configs:
  - job_name: "busos-api"
    static_configs:
      - targets: ["busos-api:8000"]
        labels:
          environment: "production"
          service: "busos-api"
